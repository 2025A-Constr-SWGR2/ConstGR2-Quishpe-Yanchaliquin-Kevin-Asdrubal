name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: [self-hosted]
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - ejecutar: echo "🐧 Este trabajo ahora se está ejecutando en ${{ runner.os }} servidor alojado por GitHub!"
      - ejecutar: echo "🔎 El nombre de su sucursal es ${{ github.ref }} y su repositorio es ${{ github.repository }}."
      - nombre: Echa un vistazo al código del repositorio
        usos: acciones/checkout@v4
      - ejecutar: echo "💡 El repositorio ${{ github.repository }} ha sido clonado al corredor."
      - ejecutar: echo " ⁇  El flujo de trabajo ahora está listo para probar su código en el corredor."
      - nombre: Lista de archivos en el repositorio
        correr: |
          ls ${{ github.workspace }}
      - ejecutar: echo "🍏 El estado de este trabajo es ${{ job.status }}."
